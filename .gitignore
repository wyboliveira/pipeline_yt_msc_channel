# — Segredos e credenciais —
.env
client_secret.json
youtube_token.json

# — Arquivos de áudio e vídeo (conteúdo do usuário) —
*.mp3
*.wav
*.flac
*.m4a
*.ogg
*.mp4

# — Pastas de trabalho do pipeline —
inbox/
processing/
review/
rejected/
logs/

# — Imagens geradas (histórico local, potencialmente volumoso) —
assets/images/

# — Python —
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# — IDEs —
.vscode/
.idea/
*.suo
*.user
